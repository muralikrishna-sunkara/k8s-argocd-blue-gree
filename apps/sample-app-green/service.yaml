apiVersion: v1
kind: Service
metadata:
  name: sample-app-green
  namespace: default
  labels:
    app: sample-app
    version: green
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
    name: http
  selector:
    app: sample-app
    version: green